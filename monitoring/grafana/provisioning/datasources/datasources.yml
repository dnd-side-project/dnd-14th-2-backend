apiVersion: 1

datasources:
  # Prometheus - 메트릭 데이터소스 (기본)
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true

  # Loki - 로그 데이터소스
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
